# CLAUDE.md — Project Context for Claude Code

This file provides context for Claude Code, Codex, and other AI agents working on this codebase.

## Project

Apex Log Insights — a TypeScript monorepo that parses Salesforce Apex debug logs into structured, analyzable data. Ships as an npm library, CLI, MCP server, and browser extensions (Chrome, Edge, Firefox).

## Required Reading

Before making any changes, read these files:

- `CONTRIBUTING.md` — Architecture, bug-fix workflows, build targets, version bumping, release process
- `STYLE_GUIDE.md` — Code style, TypeScript conventions, architecture invariants, naming rules

## Architecture — The Three-Layer Rule

```
Layer 1 — Parser      packages/core/src/certinia/   → raw event extraction
Layer 2 — Report      packages/core/src/insights/*   → structured report JSON
Layer 3 — Renderer    viewer/modules/render-*.js     → UI display
                      packages/browser-ext/app.js
```

Fix bugs at the lowest layer where they originate. Never patch the renderer to work around a parser gap. Never add format checks in render modules — that belongs in `normalize-report.js`.

## Monorepo Structure

```
packages/core/         → shared parsing engine (zero runtime dependencies)
packages/cli/          → command-line interface
packages/mcp/          → MCP server for AI tools
packages/browser-ext/  → browser extension (Chrome, Edge, Firefox)
viewer/                → offline HTML viewer (vanilla JS, no build step)
fixtures/              → shared test log files
scripts/               → build & release utilities
  assemble-extension-ui.ts → merges viewer sources into extension shared/ assets
  export-extension.ts      → full release pipeline (bump + build + zip)
```

## Key Commands

```bash
# ── Setup ──
pnpm install                    # install dependencies

# ── Develop ──
pnpm dev:cli                    # watch mode for CLI
pnpm dev:mcp                    # watch mode for MCP server
pnpm dev:ext                    # watch mode for browser extension

# ── Validate ──
pnpm lint                       # ESLint all packages
pnpm format                     # Prettier format all files
pnpm typecheck                  # TypeScript check all packages
pnpm test                       # run all tests (vitest)

# ── Build ──
pnpm clean                      # remove all dist/ folders
pnpm build                      # sync versions → build all → export extension

# ── Quality ──
pnpm audit                      # security scan (deps + secrets + eslint-security)
pnpm bugs                       # bug scan (knip + tsc strict + attw + madge)

# ── Release ──
pnpm release                    # build + publish all packages
```

## Build: Extension UI Assembly

`pnpm build` automatically assembles the extension UI via `scripts/assemble-extension-ui.ts`. This merges `viewer/app.js` and `viewer/styles.css` with extension-only overlays into `shared/`. Changes to viewer sources propagate to the extension on every full build. Never edit `shared/app.js` or `shared/styles.css` directly — they are generated.

## Building Extensions

```bash
# Rebuild with current version
pnpm --filter @apex-log-insights/browser-ext build:chrome

# Bump version and build
pnpm --filter @apex-log-insights/browser-ext build:chrome -- --version 1.2.0
```

## Version Management

Single version source in root `package.json`. Run `node scripts/sync-versions.mjs` to propagate to all packages and manifests. Or use the `--version` flag on the build scripts.

## Commit Conventions

Prefix commits with the affected layer: `parser:`, `report:`, `viewer:`, or `extension:`.

## Critical Rules

1. `normalize-report.js` is the only place that handles format differences
2. No build step for the viewer — it is zero-dependency vanilla JS ESM
3. `apex-parser-worker.js` is a build artifact — never edit directly
3b. `shared/app.js` and `shared/styles.css` are generated by `assemble-extension-ui.ts` — never edit directly
4. Parser bugs → fix in `packages/core/src/certinia/`
5. Report bugs → fix in `packages/core/src/insights/*.ts`
6. Display bugs → fix in the viewer or extension `app.js`
7. Never cross layers
8. All tests must pass before any commit

## Documentation Maintenance — MANDATORY

Every code change must include corresponding documentation updates in the same changeset.
Do not leave documentation for a follow-up — update it as part of the work.

### What to update and when

| If you change… | Update these files |
|---|---|
| Build scripts, commands, or flags | `CONTRIBUTING.md` (Building & Releasing section), `packages/browser-ext/README.md`, `README.md` (Development section) |
| Architecture, layers, or package structure | `CONTRIBUTING.md` (Monorepo Layout, Three-Layer Rule), `STYLE_GUIDE.md`, this file (`CLAUDE.md`), `AGENTS.md`, `.cursorrules`, `.windsurfrules`, `.clinerules`, `.github/copilot-instructions.md` |
| CI/CD workflows or release process | `CONTRIBUTING.md` (Release Flow section) |
| New features, bug fixes, or breaking changes | `CHANGELOG.md` (add entry under `[Unreleased]` or the current version heading) |
| Feature additions or removals | `FEATURES.md` |
| New report fields or event types | `CONTRIBUTING.md` (Adding a New Report Field / Adding a New Event Type checklists) |
| TypeScript conventions or coding patterns | `STYLE_GUIDE.md` |
| New dependencies | `README.md` (Dependency Graph), relevant `packages/*/README.md` |
| Version bumping or sync process | `CONTRIBUTING.md` (Version Bumping section) |
| Store submission or extension publishing | `packages/browser-ext/README.md`, `packages/browser-ext/store/SUBMISSION-CHECKLIST.md` |

### Agent config files

This project maintains context files for multiple AI agents. If you change the architecture,
critical rules, key commands, or package structure, update ALL of these files to stay in sync:

- `CLAUDE.md` — Claude Code (this file)
- `AGENTS.md` — Codex, Gemini, Kimi, and generic agents
- `.cursorrules` — Cursor
- `.windsurfrules` — Windsurf
- `.clinerules` — Cline
- `.github/copilot-instructions.md` — GitHub Copilot

### CHANGELOG format

Follow [Keep a Changelog](https://keepachangelog.com/) format. Group changes under:
`Added`, `Changed`, `Fixed`, `Removed`, `Breaking`. The release workflow reads the
CHANGELOG to generate GitHub Release notes — entries must be accurate and complete.

### Checklist before committing

- [ ] All affected documentation files updated
- [ ] CHANGELOG.md has an entry for this change
- [ ] Agent config files updated (if architecture/rules/commands changed)
- [ ] `pnpm test && pnpm typecheck` pass
